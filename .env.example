# Where the room server lives.
#
# Local:      leave unset — the client falls back to 127.0.0.1:8787 (wrangler dev)
# Production: the deployed Worker's hostname, no protocol
#
# NEXT_PUBLIC_PARTY_HOST=guardian-jeopardy-room.<your-subdomain>.workers.dev
